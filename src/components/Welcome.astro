---
import Container from "@components/Container.astro"
import PrismText from "@components/PrismText.astro"
import Heading from '@components/Heading.astro'
import SocialMedia from "@components/SocialMedia.astro"
import Image from "@components/Image.astro"

import PrimaryImage from '@images/laptop.jpg'
import SecondaryImage from '@images/laptop.jpg'
---

<section class="section">
  <Container>
    <div class="socials">
      <SocialMedia/>
    </div>
    <Heading element="h1">
      Hey, I'm Grayson.
    </Heading>
    <Heading element="h2" appearance="h1">
      Front-End <PrismText>Developer</PrismText>,<br/>nature lover, amateur traveler.
    </Heading>
  </Container>
  <Container size="lg">
    <div class="layout">
      <div class="primary-cell">
        <Image
          class="primary-image"
          src={PrimaryImage}
          alt="Laptop with code on screen"
        />
      </div>
      <div class="secondary-cell">
        <Image
          class="secondary-image"
          src={SecondaryImage}
          alt="Laptop with code on screen"
        />
      </div>
      <div>
        <p class="text">
          I'm a motivated and enthusiastic web developer who studied full-stack web development at Georgia Tech's Web Development Boot Camp.
        </p>
      </div>
      <div>
        <p class="text">
          I enjoy learning techniques, methodologies, and best practices. That directly raises the standards I have for my work.
        </p>
      </div>
      <div>
        <p class="text">
          I have created many projects throughout my career, during my free time, and in my studies. I've wrote about a few of my favorites below.
        </p>
      </div>
    </div>
  </Container>
</section>

<style>
  .section {
    margin-bottom: var(--spacing--lg)
  }

  .socials {
    margin-bottom: var(--spacing--md);
  }

  .layout {
    display: grid;
    gap: 3rem;
    margin-top: var(--spacing--lg);
  }

  .primary-image {
    max-height: 24rem;
    object-fit: cover;
  }

  .secondary-cell {
    display: none;
  }

  .secondary-image {
    object-fit: cover;
  }

  .text {
    text-wrap: pretty;
  }

  @media (width >= 48rem) {
    .layout {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      grid-template-rows: repeat(3, 1fr);
    }

    .primary-cell {
      grid-column: 1 / 2;
      grid-row: 1 / -1;
    }
  }

  @media (width >= 48rem) and (width < 62rem) {
    .primary-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }

  @media (width >=62rem) {
    .layout {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      grid-template-rows: auto;
    }

    .primary-cell {
      grid-column: 1 / 3;
      grid-row: 1 / 2;
    }

    .secondary-cell {
      display: block;
      grid-column: 3 / 4;
      grid-row: 1 / 2;
    }

    .secondary-image {
      width: 100%;
      height: 100%;
    }
  }
</style>
